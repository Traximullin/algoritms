{

    function splitTens(n: number) {
        return [Math.floor(n / 10), n % 10];
    }

    
    function multiply(num1: string, num2: string): string {
        const res = [];

        for (let i = num1.length - 1; i >= 0; i--) {
        }
    };

    console.log(multiply("438", "597"))
}