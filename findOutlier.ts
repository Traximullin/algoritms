
const helper = (number: number): boolean => {
    return !!(number % 2)
}

const findOutlier = (integers: number[]) => {

    integers.forEach((number,index) => {
        if(index === 0) {
        }
    })
}

console.log(findOutlier(arr))